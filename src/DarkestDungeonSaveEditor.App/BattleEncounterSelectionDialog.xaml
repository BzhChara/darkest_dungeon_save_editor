<Window x:Class="DarkestDungeonSaveEditor.App.BattleEncounterSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:local="clr-namespace:DarkestDungeonSaveEditor.App"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{local:Text BattleEncounterSelectionDialog_018}"
        Width="1180"
        Height="720"
        MinWidth="900"
        MinHeight="560"
        Icon="Assets/save-editor.ico"
        Background="{StaticResource AppTextureBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        FontFamily="{x:Static local:EditorTypography.Body}"
        FontSize="14"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Grid Background="{StaticResource AppTextureBrush}">
        <Border Margin="8"
                Background="#90050505"
                BorderBrush="{StaticResource GoldDarkBrush}"
                BorderThickness="1">
            <Border Margin="3" BorderBrush="#4B5351" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="78" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0"
                            Padding="21,9"
                            Background="{StaticResource HeaderBrush}"
                            BorderBrush="#59615E"
                            BorderThickness="0,0,0,1">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock x:Name="HeaderTitleTextBlock"
                                       Style="{StaticResource PageTitleStyle}"
                                       FontSize="27"
                                       Text="{local:Text BattleEncounterSelectionDialog_019}" />
                            <TextBlock Margin="1,2,0,0"
                                       Style="{StaticResource KickerTextStyle}"
                                       Text="{local:Text BattleEncounterSelectionDialog_020}" />
                        </StackPanel>
                    </Border>

                    <Grid Grid.Row="1" Margin="16,12,16,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="170" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="360" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Margin="2,0,10,0"
                                   Style="{StaticResource FieldLabelStyle}"
                                   Text="{local:Text BattleEncounterSelectionDialog_021}" />
                        <ComboBox x:Name="DifficultyComboBox"
                                 Grid.Column="1"
                                 Margin="0"
                                 DisplayMemberPath="DisplayName"
                                 SelectionChanged="DifficultyComboBox_SelectionChanged"
                                 ToolTip="{local:Text BattleEncounterSelectionDialog_022}" />
                        <TextBlock Grid.Column="2"
                                   Margin="16,0,10,0"
                                   Style="{StaticResource FieldLabelStyle}"
                                   Text="{local:Text BattleEncounterSelectionDialog_023}" />
                        <TextBox x:Name="SearchTextBox"
                                 Grid.Column="3"
                                 Margin="0"
                                 TextChanged="SearchTextBox_TextChanged"
                                 ToolTip="{local:Text BattleEncounterSelectionDialog_024}" />
                        <TextBlock x:Name="CountTextBlock"
                                   Grid.Column="4"
                                   Margin="16,0,2,0"
                                   VerticalAlignment="Center"
                                   TextAlignment="Right"
                                   Foreground="{StaticResource GoldBrush}"
                                   FontWeight="Bold" />
                    </Grid>

                    <DataGrid x:Name="EncounterGrid"
                              Grid.Row="2"
                              Margin="16,0"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              HeadersVisibility="Column"
                              SelectionMode="Single"
                              SelectionUnit="FullRow"
                              SelectionChanged="EncounterGrid_SelectionChanged"
                              MouseDoubleClick="EncounterGrid_MouseDoubleClick"
                              RowHeight="52"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="{local:Text BattleEncounterSelectionDialog_025}" Width="2.6*" MinWidth="310">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Center"
                                                    ToolTip="{Binding CompositionToolTip}">
                                            <TextBlock Text="{Binding PreferredComposition}"
                                                       Foreground="{StaticResource TextPrimaryBrush}"
                                                       FontWeight="Bold"
                                                       TextTrimming="CharacterEllipsis" />
                                            <TextBlock Margin="0,2,0,0"
                                                       Text="{Binding Composition}"
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       FontFamily="Consolas, Microsoft YaHei UI"
                                                       FontSize="12"
                                                       TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="{local:Text BattleEncounterSelectionDialog_026}" Binding="{Binding SourceKind}" Width="96" ElementStyle="{StaticResource DataGridTextElementStyle}" />
                            <DataGridTextColumn Header="{local:Text BattleEncounterSelectionDialog_027}" Binding="{Binding TargetKind}" Width="82" ElementStyle="{StaticResource DataGridTextElementStyle}" />
                            <DataGridTextColumn Header="{local:Text BattleEncounterSelectionDialog_028}" Binding="{Binding DungeonId}" Width="115" ElementStyle="{StaticResource DataGridTextElementStyle}" />
                            <DataGridTextColumn Header="{local:Text BattleEncounterSelectionDialog_029}" Binding="{Binding Source}" Width="1.8*" MinWidth="220" ElementStyle="{StaticResource DataGridTextElementStyle}" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <TextBlock x:Name="SelectionTextBlock"
                               Grid.Row="3"
                               Margin="18,10,18,0"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Text="{local:Text BattleEncounterSelectionDialog_001}"
                               TextTrimming="CharacterEllipsis" />

                    <Grid Grid.Row="4" Margin="16,14,16,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="1"
                                MinWidth="104"
                                Margin="0,0,10,0"
                                Style="{StaticResource GhostButtonStyle}"
                                Content="{local:Text BattleEncounterSelectionDialog_030}"
                                IsCancel="True"
                                Click="Cancel_Click" />
                        <Button x:Name="ConfirmButton"
                                Grid.Column="2"
                                MinWidth="150"
                                Margin="0"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Content="{local:Text BattleEncounterSelectionDialog_031}"
                                IsDefault="True"
                                IsEnabled="False"
                                Click="Confirm_Click" />
                    </Grid>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Window>
