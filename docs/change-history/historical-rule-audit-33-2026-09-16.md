# 历史修改第三十三轮审核（2026-09-16）

## 提交与结论

按用户要求，先提交上一轮物品引用修复及配套测试、规则和记录：`0047219`（`fix: resolve item references after provider and wallet selection`）。提交前核对上一轮 `verification.json` 中的 17 个任务文件和 3 个 Release 程序集哈希，均一致；`git diff --check` 通过，提交后工作区干净。

本轮继续只读检查历史实现，**未确认新的、具备可复现依据的产品错误，也没有确认应直接删除的兼容或保护分支**。这不是对所有游戏资源、Mod 和运行状态无缺陷的证明。

## 实际检查范围

- **物品身份与来源**：原始 `(type,id)` first-match、钱包聚合、原生哈希冲突保护、官方来源保留、类型引用和货币引用的独立入口；核对缺失清单文件经过覆盖选择后再影响完整性的路径。目录键已使用类型长度前缀，不能再按旧的分隔符拼接问题报告。
- **增量刷新与保存**：`QuantityItemCatalog.RefreshSavedAmounts`、保存条目读取、`ProfileCatalogSnapshotReader` 的场景／资源缓存失效，以及 `SaveEditService.QuantityItems` 的预览、提交前后定义校验、只读残留保护和内容锁。用途可疑与保存身份不可写是不同状态，未把“隐藏物品”误当成保存禁用条件。
- **引用消费者**：Loot、任务／事件／费用 JSON、升级树、人物／怪物标准路径和 Curio CSV 的分流；核对上下文和已验证／未验证线索的边界。普通 Building 数据仍是不确定线索，不能把未知触发流程直接改成确认引用。
- **相邻历史保护**：物品容量的最终字段赋值、人物初始 HP 条件组合、地图奇物资源与 CSV 的阶段关系、强制回城／战斗历史的路径和版本校验。这里只读追查相关入口与条件，没有重新执行完整游戏机制研究。
- **日志与同步发布**：检查物品刷新后的诊断如何被主界面消费；仅数量更新不会重新发布整套资源诊断。没有把缓存刷新时 `Issues` 的变化单独认定为玩家已丢失错误提示或资源已被误判恢复。

## 新增隔离验证

探针引用已提交版本对应的 Release Core DLL，不引用产品工程，不重新构建产品或旧研究探针。所有资源和 JSON 对象位于新的 `workspaces/historical-rule-review33-20260916/`；没有操作真实档案、游戏、Mod 或 Steam。

对 Base／本地 Mod／工坊 Mod × 小镇／副本 × 完整／缺失定义清单条件，分别执行四步存档变化，累计 **48 次增量刷新与完整重读对照**。Base 不使用 Mod 清单，两个缺失标签下作为不受清单影响的重复控制。

检查内容包括：

1. 新增数量为零的保存记录，验证“存在于存档”不等于“数量大于零”。
2. 两条相同金币身份合计为 13，大小写不同的传家宝身份仍各自独立；小镇金币聚合与副本金币变体保持各自规则。
3. 新增仅存档物品时，定义读取不完整的只读标志与完整重读一致；资源完整时不会误加该保护。
4. 删除保存记录后，仅存档残留消失，有定义的行回到正确的零数量／隐藏状态；重新出现时更新记录数和数量。
5. 副本饰品不泄漏到普通数量物品列表，但仍占实际背包格数；读取不改动输入 JSON。

比较了目录键、原始身份、数量、堆叠上限、配给标记、保存记录数、存在性、仅存档标记、默认隐藏状态、提供者冲突、引用状态、来源路径、定义读取失败列表、场景和保存哈希。没有用“完整重读和增量结果一致”替代所有字段的原生游戏证明；本轮没有新增 DSON 提交或实机实验。

首次 `run1.log` 因探针使用仅按 `PersistedType=gold` 的 `Single`，遇到副本两条合法金币变体而抛异常，退出码 `-532462766`。这是探针断言错误，不能算产品失败。修正为副本精确选择 `id=variant`，并补上探针顶层异常捕获后，`run2.log` 退出码 0，48 次全部通过，结果见 `run2/results.json`。

上一轮完整回归为 157 组通过，本轮未重复运行；当前产品程序集与该验证版本一致。当前轮是只读审核、没有产品代码 diff，按 AGENTS 不触发实施完成后的独立审查员。

## 建议

保留缺失有效提供者保护、原生哈希冲突保护、保存身份长度／NUL 校验、只读残留保护、资源变更校验和事务回滚。此次检查未证明它们属于已经过时的兼容逻辑，直接删除会失去已有验证的边界。

如继续拓展，应优先增加真实 Mod 或用户可复现案例，再决定是否需要调整尚未完整模拟的触发／重复声明规则。本轮不改产品规则，也不把尚未验证的机制当成已发现的 bug。

探针源码、运行日志、逐项结果和最终文件校验均保存在 `workspaces/historical-rule-review33-20260916/`。本报告及索引是提交后的新增审查记录，尚未再次提交。
