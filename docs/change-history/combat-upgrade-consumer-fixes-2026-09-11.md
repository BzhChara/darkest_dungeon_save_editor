# 人物战斗技能购买目标与解锁校验修复：2026-09-11

用户确认修复 [第十五轮审核](combat-upgrade-consumer-audit-2026-09-11.md) 的两项问题。基线为 `c44aea4`。本轮修改产品代码、回归测试和规则文档；未修改真实档案或 Mod，未提交新的 Git commit。

## 最终行为与范围

- 新增 `HeroSkillPurchaseTargets.cs`：共用战斗／露营的原生 63 字节购买目标计算与身份保护。技能本身仍保留完整 ID；通用资源 hash 不变。战斗输入本身超出已验证 63 字节边界、NUL、UTF-8 截断不完整、不同技能目标重合或 hash 冲突时明确禁止生成。露营原有支持范围和 code `0` 保持不变。
- `HeroClassCatalog.Progression.cs` 以战斗查询的有界目标绑定实际树；完整长树不再替代原生短目标。无精确树名但存在不同原始树 ID 的 hash 别名时明确阻止，保留冲突来源。异常目标不会中断其他职业的目录加载，生成预检会显示具体限制。
- `StagecoachHeroCandidateFactory.Progression.cs` 和 `ImplicitSkills.cs` 将原技能 ID 与购买目标分开传递；按正确目标写入显式／隐式购买，按完整技能 ID 查找档数和多数规则。无树分支不再从截断树名反推技能。
- 新增 `StagecoachHeroCandidateFactory.CombatUpgrades.cs`：已有树必须在当前人物等级能购买基础码 `0`。只有 `A/a`、空树或尚未满足基础码门槛时拒绝生成。存在 `0,2` 这样的中间空洞时保留作者定义的全部可买记录，并提示游戏实际只能读取第 0 档（技能 1 级）；不擅自补写 `1`。

影响目录可生成等级、候选预览、技能购买记录及提交前计划重算。沿用现有内容指纹和保存事务；未改变装备等级与 HP、技能装备数量、怪癖规则、怪物组合／Bridge、地图编辑、清单读取和同路径覆盖，也不迁移旧人物的购买记录。

## 测试调整

新增战斗目标和购买码两个合同模块，挂入完整套件及 `--semantics`。覆盖六种来源、ASCII／UTF-8 边界、只有长树／只有原生树／两者并存、无树基础解锁、长 ID 的多数规则、异常目标、hash 别名、异常生效树及不改清单的内容刷新。

旧通用人物样例将字母 `a` 当成基础解锁，现改为真实基础码 `0`，仍保留其他大小写不同的 code；同步更新目录、候选、可用性及保存断言。逐树覆盖测试保留 `a/A`，但为可生成样例补齐基础码 `0`。新增独立反例继续断言只有 `a/A` 时应拒绝，避免把“原样读取／保存”误当成“战斗技能已解锁”。装备与通用 writer 不因此禁止字母。

## 验证

执行产物位于 `workspaces/combat-upgrade-consumer-fixes-20260911/`。

- 旧实现的红灯验证命中 `base/ascii-64-full`：仍绑定完整长名称，见 `red.log`。
- Release 全方案构建成功，0 警告／0 错误，见 `build.log`。首次受限环境构建失败但没有编译错误详情；在授权环境重跑相同构建成功，原日志保留为 `build-sandbox.log`。
- 定向 `--semantics`：**16 PASS / 0 FAIL / 0 SKIP**，退出 0。
- 完整回归：**48 PASS / 0 FAIL / 0 SKIP**，退出 0；包括人物生成、物品饰品、自动同步、战斗与 Bridge、地图操作、强制回城、保存事务和回滚。
- 新增目标矩阵 60 组、购买码矩阵 66 组，另有异常身份／别名／生效树／重复技能等 8 个保护场景目录（其中一个目录验证两个直接输入边界）和有界目标多数规则对照。
- 对本轮新增的本地样例完成 **48 次实际 DSON 往返**。`verify_fix.py` 再以本机 UCRT 核对 60 组格式化目标，并检查保存 hash、原购买码、GUID、原始技能选择及原生连续购买码模型的可达档数；定向和完整套件产物校验均通过。该模型不是在游戏内调用函数。
- Core、App 与正式测试目录的核心 DLL SHA-256 一致：`80A38BE5433C794030902FAAAD9732C27CB37FB42973A1862DF0744AC34417BF`。验证前后 15 份代码／测试文件 hash 一致；规则说明与修改记录另行保存。

初次新增测试编译缺少 `CultureInfo` 引用，补充后完成红灯验证。第一次修复后测试把 NUL 放进文本定义，但原生文本读取先在 NUL 终止，无法到达预期的身份校验入口；改为直接调用生成预检验证该边界，保留文本解析规则。两次测试问题的原始日志分别保存在 `test-compile-initial.log`、`semantics-initial.log`；最终测试没有跳过这些验证。

本轮未新开游戏实测，也未迁移已有存档或重写用户 Mod。原生结论依据固定版本反汇编、UCRT 格式化调用和隔离存档对照。

独立只读复核已完成，审查者 `01a08f46-0be9-7961-9432-ebef7774ce1d`（全新上下文）未发现本次差异中可复现的实质问题。覆盖全部 15 份代码／测试文件及文档，核对原生片段、目录至提交前重算／保存路径、验证日志与 DLL hash；另外完成 8 组纯内存边界检查，并分别复核定向／完整两次运行的 UCRT 与 DSON 产物。未重跑构建或整套测试，未进行游戏实测，未修改文件。审查者已关闭；本轮没有待处理的实质审查问题。
